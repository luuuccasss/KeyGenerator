# config.py

mysql_config = {
    'host': 'localhost', # IP of your database
    'user': 'user', # Use of your database
    'password': 'password', # Password of your Database
    'database': 'database' # Name of your Database
}

# Define the key pattern
key_pattern = 'XXXXX-XXXXX-XXXXX'  # Change this to specify the key pattern

# Define what should be included in the keys
key_content = 'numeric'  # 'numeric' for numbers only, 'alphabetic' for letters only, 'alphanumeric' for both
